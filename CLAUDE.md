# CLAUDE.md — red-wiki 운영 규약 ({{REPO_KEY}})

> 이 저장소는 **{{SCOPE}}**
> 여기 없는 것을 여기서 찾지 않는다. 다른 저장소의 문서를 고쳐야 하면 그 저장소를 연다.
> 무엇이 열려 있는지는 카탈로그(`index.md`)가 말한다 — 앱이 만들고 앱 폴더에 둔다.
>
> red-wiki는 **red 서비스의 지식(명사)** 만 담는다.
> **이 파일은 매 세션 전부 로드된다.** 그래서 여기에는 어느 작업에서든 필요한 것만 둔다 —
> 라우팅 · 경계 · 3층 · 마무리. 나머지는 `.claude/rules/` 가 해당 파일을 만질 때 알아서 뜬다.
> 여기에 새 규칙을 더하기 전에 그 규칙이 **모든** 작업에 필요한지 먼저 묻는다. 아니면 rules 로 간다.

| 규칙 파일                      | 언제 뜨나                                      |
| ------------------------------ | ---------------------------------------------- |
| `.claude/rules/authoring.md`   | `docs/**/*.md` — 문서를 만들거나 고칠 때       |
| `.claude/rules/frontmatter.md` | `docs/**/*.md` — frontmatter·원천·변경 이력    |
| `.claude/rules/app.md`         | `docs/ops/**` — 앱이 하는 일 · 코드는 여기 없다 |
| `.claude/rules/repo-files.md`  | `registry.yaml` · `index.md`                   |
| `.claude/rules/proposal-block.md` | `.claude/skills/**` — 제안 블록 문법         |
| `.claude/writing-style.md`     | 문체·구조 규칙 (문서를 쓰기 전후로 검토한다)   |

> **이미 있는 위키에 저장소를 하나 더 붙이는 것이라면 두 가지를 지운다.**
>
> 1. `.claude/` 의 `skills/` · `rules/` · `writing-style.md` — 규약은 기본 저장소가 하나만 갖고,
>    앱이 열려 있는 저장소 전부의 것을 합쳐 준다. 사본을 들고 있으면 규약 한 줄을 고칠 때마다
>    저장소 수만큼 맞춰야 한다. 남기는 것은 `settings.json` 뿐이다 — 터미널에서 이 저장소를
>    직접 열 때 쓰는 권한이라 합쳐지지 않는다.
> 2. `docs/ops/` — 위키를 굴리는 규칙이라 기본 저장소 것이다. 자리가 남아 있으면 거기에
>    ops 문서를 쓰게 되고, 그러면 같은 규칙이 두 곳에 생긴다.
>
> 남는 것은 `docs/product/`(이 저장소가 맡은 도메인)와 `docs/decisions/`(그 도메인의 결정)다.
> 이 저장소가 **첫 저장소**(규약을 들 저장소)라면 둘 다 그대로 둔다.

---

## 0. 호칭

이 프로젝트에서 나를 부르는 이름은 **레드키위**다(성 "레드" · 이름 "키위"). 자기소개·1인칭 지칭에 쓴다. `docs/**` 본문에는 넣지 않는다 — 대화상의 호칭이다.

## 1. 라우팅 — 이 말이 나오면 이걸 먼저 읽어라

문서를 grep으로 뒤지기 전에 이 표를 본다. 순서를 지킨다.

| 사용자가 이런 말을 하면                                     | 이 순서로 읽는다                                                                                       |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| "무슨 시스템·도메인이 있어", "이 기능 어느 영역이야"        | `registry.yaml` 의 `systems:` · `domains:` — 이 저장소가 아는 **어휘**가 거기 있다                      |
| "이거 뭐 하는 거야", "기능", "스펙", "요구사항"             | `index.md` 카탈로그로 그 문서를 찾는다 → `docs/product/`                                                 |
| "이 용어 뭐야", "이 개념", 약어·도메인 명칭                 | 용어집이 원천이다. 없으면 `docs/product/` 를 훑는다                                                      |
| **"A 랑 B 차이가 뭐야", "이건 어느 영역이야"** — 경계·구분  | `registry.yaml` 의 `domains:` 로 축을 잡고 용어집을 본다. **열려 있는 저장소 전부가 근거다**             |
| 아무 질문이나 — 근거를 대서 답해야 할 때                    | `/ask` 스킬. `index.md` 부터 읽고 인용해 답한다                                                          |
| "이미지 넣었어", "이 스크린샷 문서에 붙여줘"                 | `.claude/rules/authoring.md` 의 원천 절 — 업로드는 앱의 편집 폼이 한다                                   |
| "이 문서 언제 바뀌었어", "누가 고쳤어"                      | 짝 파일 `<문서>.history.yaml` — 본문 md 밖에 있다                                                        |
| "왜 그렇게 정했어", "이거 언제 결정했지", "그때 뭐라 했더라" | `docs/decisions/` — 대화에서만 나온 것. 새로 남길 때는 `/decision` 스킬                                  |
| "문서 어떻게 써", "문체", "이 문장 이상해"                  | `.claude/writing-style.md`                                                                               |
| "노션 문서 가져와"                                          | 앱의 **[동기화]**. 노션이 원본인 문서(`mirror: notion`)는 여기서 고치지 않는다                           |
| "문서 하나 고쳤는데 관련된 것도"                            | `/change-propagation` 스킬                                                                               |
| "앱 고쳤어", "명령 바꿨어" — 뷰어 코드를 건드렸을 때        | 뷰어는 별도 레포다(`offen-red/red-wiki-desktop`). 여기서는 `docs/ops/` 만 고친다                         |
| 전체 목록·현황이 필요할 때                                  | `index.md` (앱이 만드는 카탈로그. 이 저장소가 아니라 앱 폴더에 있다)                                     |
| 위키 자체를 어떻게 굴리나                                   | `docs/ops/`                                                                                              |

이 표는 **이 저장소에 실제로 있는 문서만** 가리켜야 한다. 문서를 늘리면 여기에 한 줄 더한다 —
없는 파일을 가리키는 줄은 지운다. 없는 것을 찾으러 가는 것이 가장 비싼 실수다.

원천이 없거나 문서에 근거가 없으면 **지어내지 않고 "관련 기록 없음"이라고 답한다.**

### 결정으로 굳는 말은 그 자리에서 묻는다

사용자가 **"그렇게 하자" · "A로 가자" · "안 하기로" · "정했어" · "이걸로 간다"** 라고 하면 답 끝에 한 줄을 붙인다.

> 결정으로 남길까요? (무엇을·왜·버린 대안만 주시면 나머지는 채웁니다)

예라고 하면 `/decision`. **판단하는 것은 말투 하나다** — 중요해 보인다고 묻지 않는다. 중요도를 AI 가 재기 시작하면 매 턴 물어보게 되고, 세 번이면 아무도 안 읽는다. **두 번 묻지 않는다.** 한 번 물어 답이 없으면 넘어간다.

이 규칙이 `/decision` 이 아니라 여기 있는 이유: 스킬은 부를 때만 로드된다. 스킬 안에 두면 부르기 전에는 영영 뜨지 않는다.

## 2. 이 레포에 없는 것 — 여기 쓰려 하면 멈춘다

red-wiki는 **명사**(무엇인가)만 담는다. **동사**(어떻게 하나)는 각 코드 레포에 있다.

| 이런 걸 요청받으면                | ✋ red-wiki 아님          |
| --------------------------------- | ------------------------- |
| 코드 규약, 린트·포맷 규칙, 네이밍 | 해당 코드 레포 `.claude/` |
| 로컬 실행·빌드·배포·릴리스 절차   | 해당 코드 레포 `.claude/` |
| PR·티켓·테스트·버전업 워크플로    | 해당 코드 레포 `.claude/` |
| 레포 내부의 구조·기술 결정(ADR)   | 해당 코드 레포            |
| 인시던트 대응 런북                | 해당 코드 레포            |

**판별선: "레포가 하나 늘거나 사라져도 이 문서가 살아있나?"**
예 → red-wiki. 아니오 → 그 레포. 애매하면 그 레포(중복보다 낫다).

코드 레포 쪽 문서가 필요하면 **red-wiki에 새로 쓰지 말고 그 레포를 가리킨다.** 예외는 red-wiki **자신을** 굴리는 동사뿐이다(`.claude/`).

## 3. red-wiki란 — 3층

| 층                 | 무엇                    | 누가 소유 | 위치             | 지우면          |
| ------------------ | ----------------------- | --------- | ---------------- | --------------- |
| **원천 (불변)**    | 문서에 붙는 **이미지**  | 원천 도구 | `docs/_sources/<문서 폴더>/` | 정보 소실       |
| **사람 문서**      | red가 무엇인가          | 사람      | `docs/product/`  | 정보 소실       |
| **결정**           | 왜 그렇게 정했나        | 사람      | `docs/decisions/` | 정보 소실       |

```
사람이 이미지를 던진다  (편집 폼에 끌어 놓으면 앱이 그 문서의 원천 폴더에 넣는다)
        ↓  /draft
AI가 docs/product/ 에 문서를 제안하고 사람이 저장한다
        ↓  /ask
사람과 AI가 **열려 있는 저장소 전부**를 컨텍스트로 질문한다
```

**사람에게 스키마를 요구하지 않는 것이 이 설계의 목적이다.**
`_sources/` 에 있는 파일은 절대 손으로 고치지 않는다. 충돌하면 원천이 정답이다.

**`_sources/` 에는 이미지만 들어간다.** 엑셀·정의서 같은 문서형 원천은 넣지 않는다 — 원본은 노션·OneDrive 에 두고 `registry.yaml` 의 `origin:`·`url:` 로 가리킨다. 노션이 원본인 문서는 미러로 받아 온다(앱의 [동기화]).

## 4. 경로 규칙

- **마크다운만.** 첨부(이미지)는 `docs/_sources/` 에 둔다 — 앱은 `docs/` 아래만 읽으므로 그 밖은 렌더되지 않는다.
  예외 둘: 목업 `.html`(화면 폴더), 변경 이력 `.history.yaml`(모든 문서의 짝 파일). 둘 다 `.md` 가 아니라 카탈로그·사이드바에 잡히지 않는다.
- **`_sources/` 아래는 문서 하나가 폴더 하나를 갖는다.** 이름은 그 문서 경로의 `/` 를 `-` 로 바꾼 것이다 — `docs/ops/tooling/workflow.md` → `docs/_sources/ops-workflow/`. 규칙은 앱의 `sourceDirFor` 하나가 정하고 앱이 폴더를 만든다. **손으로 폴더를 만들지 않는다.** 저장할 때 어긋난 배치가 잡힌다.
- **경로는 영문 kebab-case.** 한글 경로는 URL·grep·크로스 레포 임포트에서 깨진다. `docs/_sources/` 의 **파일명**만 예외다(사람이 던지는 파일에 규칙을 요구하지 않는다) — 폴더는 예외가 아니다.
- 사이드바 라벨은 **존·폴더만 영문**(Product · Systems · Common), **문서는 한글**. `title:` 도 한글.
- **`icon:` 은 쓰지 않는다.** 문서마다 기준이 갈려 구조를 읽기 어렵게 만든다.
- `_` 로 시작하는 폴더는 카탈로그·사이드바에서 자동 제외된다.

폴더가 무엇을 담는지는 카탈로그(`index.md`)가 자동으로 보여준다. 트리를 이 파일에 적어 두지 않는다 — 폴더가 바뀔 때마다 낡는다.

## 5. frontmatter — 사람은 3개만 채운다

```yaml
---
title: 문서 이름 # 필수 — 사이드바에 보이는 이름
status: 작성중 # 필수 — 작성중 | 완료
owner: {{OWNER}} # 필수
---
```

**나머지는 AI가 채운다. 사람은 비워도 된다.** 전 필드와 원천·변경 이력 규약은 `.claude/rules/frontmatter.md` 에 있다.

## 6. 마무리 — 모든 작업 공통

문서를 만들거나 고쳤으면 끝내기 전에 이 넷을 한다.

1. **카탈로그(`index.md`)는 앱이 자기 폴더에 만든다.** 이 저장소에는 카탈로그도 명령도 없다 — 문서와 `registry.yaml` 뿐이다.
   손으로 파일을 고쳤다면 앱에서 그 문서를 한 번 저장하거나, 앱을 다시 켜면 맞춰진다.
2. **경위는 커밋 메시지에 남긴다.** 무엇을 했는지는 diff 가 말하므로 **왜 했는지**를 쓴다. 위키 안에 기록 파일을 따로 두지 않는다 — 두 벌이 되면 한쪽이 낡는다.
3. **앱 동작이 바뀌었다는 소식을 들었으면 짝이 되는 `docs/ops/` 문서를 같은 작업에서 고친다.** 짝 표는 `/change-propagation` 스킬에 있다.
4. 보고 — 만들거나 고친 문서 목록, `❓확인 필요` 를 남긴 지점 **전부**, 이어서 돌려야 할 스킬.

앱이 저장할 때 검사하는 것: frontmatter `title` 누락 · `registry.yaml` 어휘의 모양.

## 7. 글쓰기 규칙

전문은 `.claude/writing-style.md` 에 있다. 문서를 쓰거나 고칠 때 그 목록으로 검토한다. 요점은 셋이다.

- **규칙만 쓴다.** 그 규칙을 고른 이유·버린 대안·이전 구조와의 차이는 문서에 쓰지 않는다. 경위는 커밋 메시지에 남긴다.
- **추측 금지.** 불확실하면 `❓확인 필요: ...` 로 남기고 사용자에게 묻는다. 채워 넣지 않는다.
- 헤딩·구조를 안정적으로 유지한다(LLM이 재파싱한다). 한 문장에 한 사실. 표로 쓸 수 있으면 표로 쓴다.

문체는 대상에 따라 갈린다. 사람이 읽는 `docs/` 는 합니다체, LLM이 읽는 `CLAUDE.md`·`.claude/` 는 한다체. 한 문서 안에서 섞지 않는다.
