# CLAUDE.md — {{REPO_KEY}}

> 이 저장소는 **{{SCOPE}}**
> 여기 없는 것을 여기서 찾지 않는다. 다른 저장소의 문서를 고쳐야 하면 그 저장소를 연다.
> 무엇이 열려 있는지는 카탈로그가 말한다 — 앱이 만들고 앱 폴더에 둔다.
>
> 위키 공통 규약(경계 · 3층 · 경로 · frontmatter · 문체)과 `.claude/rules/` 는 **앱이 든다.**
> 여기에 사본을 두지 않는다 — 규약을 고칠 때 저장소 수만큼 맞춰야 하고, 그 저장소를 못 받은
> 사람은 규약 없이 돌게 된다. 이 파일이 정하는 것은 **이 저장소의 범위와 라우팅**뿐이다.

## 라우팅 — 이 말이 나오면 이걸 먼저 읽어라

문서를 grep 으로 뒤지기 전에 이 표를 본다. 순서를 지킨다.

**새 저장소는 문서가 없는 채로 시작한다.** 아래는 자리만 잡아 둔 것이다 — 문서를 쓰면서
그 문서를 가리키는 줄로 바꾼다.

| 사용자가 이런 말을 하면                  | 이 순서로 읽는다                                                   |
| ---------------------------------------- | ------------------------------------------------------------------ |
| {{이 저장소가 맡은 주제}}                | `docs/product/overview.md` 로 범위를 잡고 그 문서로 간다           |
| "이 용어 뭐야", "무슨 시스템 있어"       | **이 저장소에 없다.** 규약·용어를 든 저장소를 본다                 |
| "왜 그렇게 정했어", "이거 언제 결정했지" | `docs/decisions/` — 대화에서만 나온 것. 새로 남길 때는 `/decision` |
| 아무 질문이나 — 근거를 대서 답해야 할 때 | `/ask`. 카탈로그부터 읽고 인용해 답한다                            |

이 표는 **이 저장소에 실제로 있는 문서만** 가리켜야 한다. 문서를 늘리면 한 줄 더하고,
없는 파일을 가리키는 줄은 지운다. 없는 것을 찾으러 가는 것이 가장 비싼 실수다.

원천이 없거나 문서에 근거가 없으면 **지어내지 않고 "관련 기록 없음"이라고 답한다.**
